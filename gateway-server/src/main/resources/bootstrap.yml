eureka:
  client:
    serviceUrl:
      defaultZone: http://discUser:discPassword@localhost:9000/eureka/
spring:
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config
      name: gateway
      password: configPassword
      username: configUser
